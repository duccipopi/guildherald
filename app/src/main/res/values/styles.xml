<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.Text.Title" parent="Base.TextAppearance.AppCompat.Headline">
        <item name="android:textStyle">bold</item>
    </style>

    <style name="AppTheme.Text.Subtitle" parent="Base.TextAppearance.AppCompat.Subhead" />

    <style name="AppTheme.Text.Caption" parent="Base.TextAppearance.AppCompat.Caption">
        <item name="android:textStyle">italic</item>
    </style>

    <style name="AppTheme.Button.Flat" parent="Base.Widget.AppCompat.Button.Borderless">
        <item name="background">@android:color/transparent</item>
        <item name="android:shadowColor">@android:color/transparent</item>
    </style>

    <style name="HordeTheme" parent="AppTheme">
        <item name="colorPrimary">@color/hordeColor</item>
        <item name="colorPrimaryDark">@color/hordeDarkColor</item>
        <item name="colorAccent">@color/hordeAccentColor</item>
    </style>

    <style name="AllianceTheme" parent="AppTheme">
        <item name="colorPrimary">@color/allianceColor</item>
        <item name="colorPrimaryDark">@color/allianceDarkColor</item>
        <item name="colorAccent">@color/allianceAccentColor</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
